{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["node"],
    "rootDir": "./tools" /* Specify the root directory of input files. Use to control the output directory structure with --outDir. */
  },
  "exclude": ["**/*.spec.ts", "**/*.test.ts", "./lib/**/*"],
  "include": ["tools/**/*.ts"]
}
